# Contact Book

A clean desktop contact manager built with Python's built-in Tkinter library.

## Run

Install Python 3 (including Tkinter), then open a terminal in this folder and run:

```powershell
python contact_book.py
```

Your contacts are saved privately in `contacts.json` in this folder.
